<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/ssf_bg"
    android:orientation="vertical"
    android:paddingLeft="14dp"
    android:paddingTop="12dp"
    android:paddingRight="14dp"
    android:paddingBottom="14dp">

    <ImageButton
        android:id="@+id/btnBackFeedback"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:background="@android:color/transparent"
        android:contentDescription="Back"
        android:src="@drawable/ic_back_24"
        android:tint="@color/ssf_primary_variant" />

    <com.google.android.material.card.MaterialCardView
        style="@style/Widget.SSF.Card"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:padding="0dp"
        app:cardUseCompatPadding="true"
        xmlns:app="http://schemas.android.com/apk/res-auto">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/drawer_feedback"
                android:textColor="@color/ssf_text_primary"
                android:textSize="20sp"
                android:textStyle="bold" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="@string/feedback_prompt"
                android:textColor="@color/ssf_text_secondary"
                android:textSize="14sp" />

            <RatingBar
                android:id="@+id/ratingFeedback"
                style="?android:attr/ratingBarStyle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:isIndicator="false"
                android:numStars="5"
                android:rating="0"
                android:stepSize="1"
                android:progressTint="@color/feedback_star_selected"
                android:secondaryProgressTint="@color/rating_star_unselected" />

            <EditText
                android:id="@+id/etFeedbackComment"
                android:layout_width="match_parent"
                android:layout_height="120dp"
                android:layout_marginTop="12dp"
                android:background="@drawable/bg_text_field"
                android:gravity="top|start"
                android:hint="@string/feedback_app_hint"
                android:inputType="textMultiLine"
                android:maxLines="6"
                android:padding="12dp"
                android:textColor="?android:attr/textColorPrimary"
                android:textColorHint="?android:attr/textColorHint" />

            <Button
                android:id="@+id/btnSubmitFeedback"
                style="@style/Widget.SSF.Button.Gradient"
                android:layout_width="match_parent"
                android:layout_height="46dp"
                android:layout_marginTop="12dp"
                android:text="@string/submit_feedback" />
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>
</LinearLayout>

